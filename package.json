{
  "name": "tether-monorepo",
  "version": "4.0.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:server": "bun --cwd apps/server dev",
    "build:server": "bun --cwd apps/server build",
    "start:server": "bun --cwd apps/server start",
    "lint": "biome check && bun --cwd apps/server typecheck && bun --cwd apps/desktop typecheck",
    "dev:desktop": "bun --cwd apps/desktop run tauri:dev",
    "format": "biome check --write",
    "gen:proto": "bunx --bun @bufbuild/buf@1.60.0 generate",
    "docs:dev": "vitepress dev docs",
    "docs:build": "cp install.sh docs/public/install.sh && cp install.ps1 docs/public/install.ps1 && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test:desktop": "bun --cwd apps/desktop run test"
  },
  "dependencies": {
    "bun-types": "^1.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "typescript": "~7.0.2",
    "vitepress": "1.6.4"
  }
}
